<!--pages/friends/friends.wxml-->
<view class="container">
  <view class="header">
    <view class="title">我的好友</view>
    <view class="subtitle">查看好友的公开日程</view>
  </view>

  <!-- 顶部统计提醒 -->
  <view class="stats-reminder">
    <view class="reminder-item approved">
      <view class="reminder-label">已预约成功</view>
      <view class="reminder-count">{{bookingStats.approvedCount}}</view>
    </view>
    <view class="reminder-item pending">
      <view class="reminder-label">待审批中</view>
      <view class="reminder-count">{{bookingStats.pendingCount}}</view>
    </view>
    <view class="reminder-item total">
      <view class="reminder-label">总计</view>
      <view class="reminder-count">{{bookingStats.totalCount}}</view>
    </view>
  </view>

  <!-- 顶部预约详情信息 -->
  <view class="booking-details">
    <!-- 已预约日程 -->
    <view class="booking-section" wx:if="{{bookingDetails.approvedBookings.length > 0}}">
      <view class="section-title">已预约</view>
      <view class="booking-item" wx:for="{{bookingDetails.approvedBookings}}" wx:key="id">
        <view class="booking-info">
          <view class="booking-date">{{item.date}}</view>
          <view class="booking-time">{{item.start_time}} - {{item.end_time}}</view>
        </view>
        <view class="booking-owner-badge">{{item.owner_account || item.owner_name || '好友'}}</view>
      </view>
    </view>
    
    <!-- 审批中日程 -->
    <view class="booking-section" wx:if="{{bookingDetails.pendingBookings.length > 0}}">
      <view class="section-title pending">待审核</view>
      <view class="booking-item" wx:for="{{bookingDetails.pendingBookings}}" wx:key="id">
        <view class="booking-info">
          <view class="booking-date">{{item.date}}</view>
          <view class="booking-time">{{item.start_time}} - {{item.end_time}}</view>
        </view>
        <view class="booking-owner-badge pending">{{item.owner_account || item.owner_name || '好友'}}</view>
      </view>
    </view>

    <!-- 如果没有预约信息 -->
    <view class="no-booking" wx:if="{{bookingDetails.approvedBookings.length === 0 && bookingDetails.pendingBookings.length === 0}}">
      <view class="no-booking-text">暂无预约日程</view>
    </view>
  </view>

  <!-- 搜索框 -->
  <view class="search-container">
    <view class="search-box">
      <text class="search-icon">🔍</text>
      <input class="search-input" type="text" placeholder="搜索名字、职位或标签..." />
    </view>
  </view>

  <!-- 分类筛选 -->
  <view class="category-container">
    <view class="category-list">
      <view class="category-tag active">全部</view>
      <view class="category-tag">医生</view>
      <view class="category-tag">教练</view>
      <view class="category-tag">设计师</view>
      <view class="category-tag">教师</view>
    </view>
  </view>

  <!-- 好友列表 -->
  <view class="friends-section">
    <view class="friends-list">
      <view class="friend-card" wx:for="{{friends}}" wx:key="id" bindtap="viewFriendSchedule" data-id="{{item.id}}">
        <!-- 头像 -->
        <image class="friend-avatar" src="{{item.avatar_url || 'data:image/svg+xml;base64,'}}" />

        <!-- 信息区 -->
        <view class="friend-info">
          <!-- 名字和职位 -->
          <view class="friend-header">
            <view class="friend-name">{{item.name || item.nickname}}</view>
          </view>
          <view class="friend-role">{{item.role || '好友'}}</view>

          <!-- 描述 -->
          <view class="friend-bio">{{item.bio || '暂无描述'}}</view>

          <!-- 标签 -->
          <view class="friend-tags">
            <view class="friend-tag">#标签1</view>
            <view class="friend-tag">#标签2</view>
          </view>
        </view>

        <!-- 预约按钮 -->
        <button class="book-btn" bindtap="handleBookFriend" data-id="{{item.id}}">📅</button>
      </view>

      <!-- 空状态 -->
      <view class="empty" wx:if="{{friends.length === 0}}">
        <view class="empty-icon">👥</view>
        <view class="empty-text">还没有好友</view>
        <view class="empty-desc">添加好友后可以查看他们的公开日程</view>
      </view>
    </view>
  </view>

  <!-- 底部添加好友按钮 -->
  <button class="btn-add-friend" bindtap="goToAddFriend">+ 添加好友</button>
</view>

