# 🚀 快速部署指南

## 📋 推荐方案

**🥇 第一选择：Express.js（当前方案）**
- ✅ 零改动，立即可用
- ✅ 快速上线（1-2 周）
- ✅ 成本最低（¥50-200/月）
- ✅ 适合初期用户量

---

## 🎯 三种部署方式

### 方式 1️⃣：本地开发环境（推荐新手）

**适用场景**：测试、演示、小范围使用

**步骤**：
```bash
# 1. 进入项目目录
cd /Users/anyunbin/LLM/booking

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 访问
http://localhost:3000
```

**配置小程序**：
```javascript
// app.js
const apiBaseUrl = 'http://localhost:3000'
```

**优点**：
- ✅ 无需购买服务器
- ✅ 快速测试
- ✅ 完全免费

**缺点**：
- ❌ 只能本地访问
- ❌ 不能真机调试
- ❌ 不能正式上线

---

### 方式 2️⃣：云服务器部署（推荐生产环境）

**适用场景**：正式上线、多人使用

#### 选择云服务商

**推荐排序**：

1. **阿里云 ECS**（国内最稳定）
   - 价格：¥50-200/月
   - 优点：稳定、快速、支持好
   - 官网：https://www.aliyun.com/

2. **腾讯云 CVM**（国内备选）
   - 价格：¥50-200/月
   - 优点：稳定、快速、支持好
   - 官网：https://cloud.tencent.com/

3. **DigitalOcean**（国外推荐）
   - 价格：$5-20/月
   - 优点：便宜、简单、快速
   - 官网：https://www.digitalocean.com/

4. **Heroku**（快速部署）
   - 价格：免费试用 + $7/月
   - 优点：一键部署、无需配置
   - 官网：https://www.heroku.com/

#### 部署步骤

**步骤 1：购买云服务器**
```
选择配置：
- CPU：1 核
- 内存：1-2GB
- 系统：Ubuntu 20.04 LTS
- 带宽：1-5Mbps
```

**步骤 2：连接服务器**
```bash
# 使用 SSH 连接
ssh root@your-server-ip

# 输入密码或密钥
```

**步骤 3：安装 Node.js**
```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
apt install -y nodejs

# 验证安装
node -v
npm -v
```

**步骤 4：部署项目**
```bash
# 克隆项目
git clone https://github.com/anyunbin/booking.git
cd booking

# 安装依赖
npm install

# 启动服务
npm start

# 或使用 PM2 保持运行
npm install -g pm2
pm2 start npm --name "booking" -- start
pm2 startup
pm2 save
```

**步骤 5：配置域名（可选）**
```bash
# 购买域名
# 配置 DNS 指向服务器 IP
# 配置 SSL 证书（Let's Encrypt）

# 安装 Nginx
apt install -y nginx

# 配置反向代理
# /etc/nginx/sites-available/default
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# 重启 Nginx
systemctl restart nginx
```

**步骤 6：配置小程序**
```javascript
// app.js
const apiBaseUrl = 'https://your-domain.com'
// 或
const apiBaseUrl = 'https://your-server-ip:3000'
```

**优点**：
- ✅ 可以正式上线
- ✅ 真机调试
- ✅ 多人使用
- ✅ 24/7 运行

**缺点**：
- ❌ 需要购买服务器
- ❌ 需要配置和维护
- ❌ 有一定成本

---

### 方式 3️⃣：一键部署（推荐快速上线）

**适用场景**：快速上线、无需配置

#### 使用 Heroku 部署

**步骤 1：注册 Heroku**
```
访问 https://www.heroku.com/
注册账号
```

**步骤 2：安装 Heroku CLI**
```bash
# macOS
brew tap heroku/brew && brew install heroku

# Linux
curl https://cli-assets.heroku.com/install.sh | sh

# Windows
下载安装程序：https://devcenter.heroku.com/articles/heroku-cli
```

**步骤 3：登录 Heroku**
```bash
heroku login
```

**步骤 4：创建 Heroku 应用**
```bash
cd /Users/anyunbin/LLM/booking
heroku create your-app-name
```

**步骤 5：部署**
```bash
git push heroku main
# 或
git push heroku master
```

**步骤 6：查看日志**
```bash
heroku logs --tail
```

**步骤 7：配置小程序**
```javascript
// app.js
const apiBaseUrl = 'https://your-app-name.herokuapp.com'
```

**优点**：
- ✅ 一键部署
- ✅ 无需配置
- ✅ 自动扩展
- ✅ 免费试用

**缺点**：
- ❌ 免费版本有限制
- ❌ 国外服务器，国内访问慢
- ❌ 免费版本会休眠

---

## 📊 部署方式对比

| 方式 | 成本 | 难度 | 速度 | 推荐 |
|------|------|------|------|------|
| **本地开发** | 免费 | ⭐ | ⭐⭐⭐⭐⭐ | 测试 |
| **云服务器** | ¥50-200/月 | ⭐⭐⭐ | ⭐⭐⭐ | 生产 |
| **Heroku** | 免费-$7/月 | ⭐⭐ | ⭐⭐⭐⭐ | 快速 |

---

## 🎯 推荐方案

### 现在（初期）
```
使用本地开发环境
↓
测试功能
↓
收集反馈
```

### 1-2 周后（准备上线）
```
部署到云服务器或 Heroku
↓
配置小程序 API 地址
↓
提交微信小程序审核
```

### 上线后（持续运营）
```
监控服务器状态
↓
收集用户反馈
↓
快速迭代功能
↓
根据用户量决定是否升级
```

---

## 🔧 常见问题

### Q1：如何选择云服务商？

**A：根据用户位置选择**
- 国内用户 → 阿里云或腾讯云
- 国外用户 → DigitalOcean 或 AWS
- 快速上线 → Heroku

### Q2：需要多大的服务器？

**A：根据用户量选择**
- 初期（< 1,000 用户）：1GB 内存足够
- 中期（1,000-10,000 用户）：2GB 内存
- 大规模（> 10,000 用户）：4GB+ 内存

### Q3：如何保证数据安全？

**A：采取以下措施**
- 定期备份数据库
- 使用 HTTPS 加密传输
- 配置防火墙
- 定期更新依赖

### Q4：如何监控服务器状态？

**A：使用监控工具**
- PM2 监控进程
- 云服务商自带监控
- 第三方监控服务（如 New Relic）

### Q5：如何处理高并发？

**A：分阶段优化**
- 初期：单服务器 + SQLite
- 中期：单服务器 + MySQL + Redis
- 大规模：多服务器 + 负载均衡 + 微服务

---

## 📞 快速决策

### 我应该选择哪种方式？

**如果你想...**

- 🔹 **快速测试功能** → 本地开发环境
- 🔹 **快速上线** → Heroku 一键部署
- 🔹 **正式生产环境** → 云服务器部署
- 🔹 **最低成本** → 本地开发 + Heroku 免费版
- 🔹 **最稳定可靠** → 阿里云或腾讯云

---

## 🚀 立即开始

### 最快上线方案（推荐）

**第 1 步：本地测试（5 分钟）**
```bash
npm install
npm run dev
```

**第 2 步：部署到 Heroku（10 分钟）**
```bash
heroku create your-app-name
git push heroku main
```

**第 3 步：配置小程序（5 分钟）**
```javascript
const apiBaseUrl = 'https://your-app-name.herokuapp.com'
```

**总耗时：20 分钟即可上线！** ⚡

---

**版本**：1.0.0
**最后更新**：2025年11月26日
**推荐方案**：Heroku 快速上线 → 云服务器生产环境

